---
phase: 02-package-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - LICENSE
  - package.json
autonomous: true
requirements: [PKG-01, PKG-02, PKG-03, PKG-04, PKG-05]

must_haves:
  truths:
    - ".gitignore covers node_modules/, dist/, .env, and coverage/"
    - "LICENSE file exists with MIT text and author name prajwalhg"
    - "package.json has no placeholder yourusername or Your Name values"
    - ".changeset/config.json exists at the correct path"
  artifacts:
    - path: ".gitignore"
      provides: "Git ignore rules including .env"
      contains: ".env"
    - path: "LICENSE"
      provides: "MIT license file"
      min_lines: 20
    - path: "package.json"
      provides: "Package metadata with real author/URL values"
      contains: "prajwalhg"
  key_links:
    - from: "package.json"
      to: "LICENSE"
      via: "license field references MIT"
      pattern: '"license": "MIT"'
---

<objective>
Finish npm package infrastructure so the library is publishable.

Purpose: Without proper LICENSE, real metadata, and complete .gitignore, the package cannot be published to npm or distributed as open source.
Output: LICENSE file, patched .gitignore, updated package.json with real author/URLs.
</objective>

<execution_context>
@/Users/prajwalhg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prajwalhg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-package-validation/02-CONTEXT.md
@.planning/phases/02-package-validation/02-RESEARCH.md
</context>

<interfaces>
<!-- Current package.json fields to update (from codebase read): -->
<!-- author: currently "Your Name <you@example.com>" -->
<!-- homepage: currently "https://github.com/yourusername/intentmap#readme" -->
<!-- repository.url: currently "https://github.com/yourusername/intentmap.git" -->
<!-- bugs.url: currently "https://github.com/yourusername/intentmap/issues" -->

<!-- Current .gitignore contents (from codebase read): -->
<!-- node_modules/ -->
<!-- dist/ -->
<!-- coverage/ -->
<!-- *.log -->
<!-- *.tgz -->
<!-- .DS_Store -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add LICENSE, patch .gitignore, and update package.json metadata</name>
  <files>.gitignore, LICENSE, package.json</files>
  <action>
1. Create LICENSE file at project root with standard MIT license text:
   - Copyright line: "Copyright (c) 2026 prajwalhg"
   - Full MIT license text (the standard template from opensource.org)

2. Patch .gitignore to add `.env` entry. The file currently has:
   ```
   node_modules/
   dist/
   coverage/
   *.log
   *.tgz
   .DS_Store
   ```
   Add `.env` on a new line after `coverage/` (or anywhere in the file).

3. Update package.json metadata fields:
   - `"author"`: change from `"Your Name <you@example.com>"` to `"prajwalhg <prajwalgowda477@gmail.com>"`
   - `"homepage"`: change from `"https://github.com/yourusername/intentmap#readme"` to `"https://github.com/prajwalgowdahg/intentmap#readme"`
   - `"repository.url"`: change from `"https://github.com/yourusername/intentmap.git"` to `"https://github.com/prajwalgowdahg/intentmap.git"`
   - `"bugs.url"`: change from `"https://github.com/yourusername/intentmap/issues"` to `"https://github.com/prajwalgowdahg/intentmap/issues"`

Do NOT change any other fields in package.json (scripts, dependencies, exports, etc. stay untouched).

4. Verify that `.changeset/config.json` already exists at the correct path (it was moved in Phase 1). If it does not exist, check for `config.json` at root and move it. Per Phase 1 SUMMARY, this should already be done.

5. Verify that `package-lock.json` exists in the project root. If it does not, run `npm install` to generate it. It should NOT be in .gitignore.
  </action>
  <verify>
    <automated>test -f LICENSE && test -f package-lock.json && test -f .changeset/config.json && grep -q ".env" .gitignore && ! grep -q "yourusername" package.json && ! grep -q "Your Name" package.json && grep -q "prajwalhg" package.json && npm run typecheck && npm run test</automated>
  </verify>
  <done>LICENSE file exists with MIT text and prajwalhg copyright. .gitignore includes .env. package.json has real author, homepage, repository.url, and bugs.url. No placeholder values remain. All existing tests still pass.</done>
</task>

</tasks>

<verification>
- `cat LICENSE` shows MIT license with "Copyright (c) 2026 prajwalhg"
- `cat .gitignore` contains `.env` entry
- `node -e "const p=require('./package.json'); console.log(p.author, p.homepage, p.repository.url, p.bugs.url)"` shows real values with no "yourusername" or "Your Name"
- `test -f .changeset/config.json` passes (verified from Phase 1)
- `npm run test` passes (26 existing tests unchanged)
- `npm run typecheck` passes
</verification>

<success_criteria>
1. LICENSE file committed with MIT text and author "prajwalhg"
2. .gitignore includes .env alongside node_modules/, dist/, coverage/
3. package.json has zero placeholder values (no "yourusername", no "Your Name")
4. .changeset/config.json exists at correct location
5. package-lock.json exists in project root
6. All 26 existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-package-validation/02-01-SUMMARY.md`
</output>
