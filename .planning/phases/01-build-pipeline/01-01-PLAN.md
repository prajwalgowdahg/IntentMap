---
phase: 01-build-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.ts
  - src/IntentMap.ts
  - src/types.ts
  - src/matcher.ts
  - src/embeddings.ts
  - src/tokenizer.ts
  - src/adapters/react.ts
  - tests/intentmap.test.ts
  - tests/tokenizer.test.ts
  - examples/demo.ts
  - examples/SearchBar.tsx
  - .changeset/config.json
  - .changeset/README.md
  - package.json
autonomous: true
requirements:
  - BLD-01
  - BLD-02
  - BLD-03
  - BLD-04
  - BLD-05
  - BLD-06
  - BLD-07

must_haves:
  truths:
    - "npm run build produces ESM + CJS + .d.ts output in dist/"
    - "npm run typecheck passes with strict mode"
    - "npm run lint passes against src/, tests/, and examples/"
    - "npm run test passes with all 26 existing tests"
    - "dist/adapters/react.js and dist/adapters/react.cjs exist (subpath export works)"
  artifacts:
    - path: "src/index.ts"
      provides: "Barrel export for the library"
    - path: "src/adapters/react.ts"
      provides: "React hooks subpath entry"
      contains: "useIntentMap"
    - path: "tests/intentmap.test.ts"
      provides: "26 core tests"
    - path: "tests/tokenizer.test.ts"
      provides: "Tokenizer unit tests"
    - path: "examples/demo.ts"
      provides: "Usage example"
    - path: "examples/SearchBar.tsx"
      provides: "React example component"
    - path: ".changeset/config.json"
      provides: "Changeset config at correct location"
    - path: "dist/index.js"
      provides: "ESM main output"
    - path: "dist/adapters/react.js"
      provides: "ESM React subpath output"
  key_links:
    - from: "examples/demo.ts"
      to: "src/index.ts"
      via: "relative import ../src/index.js"
      pattern: "from '../src/index\\.js'"
    - from: "examples/SearchBar.tsx"
      to: "src/adapters/react.ts"
      via: "relative import ../src/adapters/react.js"
      pattern: "from '../src/adapters/react\\.js'"
    - from: "src/adapters/react.ts"
      to: "src/index.ts"
      via: "relative import ../index.js"
      pattern: "from '\\.\\./index\\.js'"
    - from: "package.json exports"
      to: "dist/index.js, dist/adapters/react.js"
      via: "exports map"
      pattern: '"\\.":.*"default":.*"./dist/index\\.js"'
---

<objective>
Restructure source files into src/, tests into tests/, examples into examples/, and verify the full build pipeline (build, typecheck, lint, test) passes.

Purpose: This is the critical path blocker for all subsequent phases. All configs already reference the target structure (src/, tests/), so the restructure is primarily a file-move operation with minimal import updates.

Output: Correctly structured project where all four tooling commands pass clean.
</objective>

<execution_context>
@/Users/prajwalhg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prajwalhg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-pipeline/01-CONTEXT.md
@.planning/phases/01-build-pipeline/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure files, update imports, and configure tooling</name>
  <files>
    src/index.ts, src/IntentMap.ts, src/types.ts, src/matcher.ts, src/embeddings.ts, src/tokenizer.ts,
    src/adapters/react.ts, tests/intentmap.test.ts, tests/tokenizer.test.ts,
    examples/demo.ts, examples/SearchBar.tsx, .changeset/config.json, .changeset/README.md,
    package.json
  </files>
  <action>
    **Prerequisite: Install dependencies.** The project has no node_modules/ and no package-lock.json. Run `npm install` first.

    **Step 1: Create target directories.**
    ```bash
    mkdir -p src/adapters tests examples .changeset
    ```

    **Step 2: Move source files to src/.**
    Move these 6 files from root to `src/`:
    - `index.ts` -> `src/index.ts`
    - `IntentMap.ts` -> `src/IntentMap.ts`
    - `types.ts` -> `src/types.ts`
    - `matcher.ts` -> `src/matcher.ts`
    - `embeddings.ts` -> `src/embeddings.ts`
    - `tokenizer.ts` -> `src/tokenizer.ts`

    **Step 3: Move React adapter to src/adapters/.**
    - `react.ts` -> `src/adapters/react.ts`

    IMPORTANT: `react.ts` currently imports `'../index.js'` and `'../types.js'`. After moving to `src/adapters/react.ts`, `../` resolves to `src/` where `index.ts` and `types.ts` will live. This is CORRECT -- do NOT change these imports.

    **Step 4: Move test files to tests/.**
    - `intentmap.test.ts` -> `tests/intentmap.test.ts`
    - `tokenizer.test.ts` -> `tests/tokenizer.test.ts`

    IMPORTANT: Test imports already reference `../src/index.js` and `../src/tokenizer.js`. These are CORRECT after the move -- do NOT change test imports.

    **Step 5: Move example files to examples/ and update their imports.**
    - `demo.ts` -> `examples/demo.ts`
    - `SearchBar.tsx` -> `examples/SearchBar.tsx`

    Update `examples/demo.ts` imports:
    - CHANGE: `from 'intentmap'` -> `from '../src/index.js'`

    Update `examples/SearchBar.tsx` imports:
    - CHANGE: `from 'intentmap/react'` -> `from '../src/adapters/react.js'`
    - CHANGE: `from 'intentmap'` -> `from '../src/index.js'`

    **Step 6: Move changeset config.**
    - `config.json` -> `.changeset/config.json`
    - Create `.changeset/README.md` with content: `# Changeset\n\nThis directory is managed by @changesets/cli.`

    **Step 7: Update lint scripts in package.json to include examples.**

    Per locked decision: "Examples included in lint (biome check covers examples/)".

    Change these scripts in package.json:
    - `"lint": "biome check ./src ./tests"` -> `"lint": "biome check ./src ./tests ./examples"`
    - `"lint:fix": "biome check --write ./src ./tests"` -> `"lint:fix": "biome check --write ./src ./tests ./examples"`
    - `"format": "biome format --write ./src ./tests"` -> `"format": "biome format --write ./src ./tests ./examples"`

    **DO NOT** modify: tsup.config.ts, tsconfig.json, vitest.config.ts, biome.json -- they already reference the correct structure.

    **DO NOT** change internal source file imports (src/*.ts) -- they all use `./` relative paths that remain valid after the move.

    **DO NOT** change test file imports -- they already reference `../src/`.

    **DO NOT** add a smoke test against dist/ output per locked decision.
  </action>
  <verify>
    <automated>ls src/index.ts src/IntentMap.ts src/types.ts src/matcher.ts src/embeddings.ts src/tokenizer.ts src/adapters/react.ts tests/intentmap.test.ts tests/tokenizer.test.ts examples/demo.ts examples/SearchBar.tsx .changeset/config.json</automated>
  </verify>
  <done>All source files in src/, React adapter in src/adapters/, tests in tests/, examples in examples/ with updated imports, changeset config at .changeset/config.json, lint scripts include ./examples</done>
</task>

<task type="auto">
  <name>Task 2: Verify full build pipeline</name>
  <files>dist/ (generated output)</files>
  <action>
    Run all four tooling commands in sequence and verify dist output structure.

    **Step 1: Build**
    ```bash
    npm run build
    ```
    This must exit cleanly. Then verify dist output:
    ```bash
    ls dist/index.js dist/index.cjs dist/index.d.ts dist/adapters/react.js dist/adapters/react.cjs dist/adapters/react.d.ts
    ```

    Per locked decision, verify these specific files exist:
    - dist/index.js (ESM main)
    - dist/index.cjs (CJS main)
    - dist/index.d.ts (TypeScript declarations)
    - dist/adapters/react.js (ESM React subpath)
    - dist/adapters/react.cjs (CJS React subpath)

    **Step 2: Typecheck**
    ```bash
    npm run typecheck
    ```
    Must exit with code 0. tsconfig already has strict mode, exactOptionalPropertyTypes, noUncheckedIndexedAccess, noImplicitReturns, noFallthroughCasesInSwitch enabled.

    **Step 3: Lint**
    ```bash
    npm run lint
    ```
    Must exit with code 0. This now checks src/, tests/, and examples/ (updated in Task 1).

    **Step 4: Test**
    ```bash
    npm run test
    ```
    Must exit with code 0. All 26 existing tests must pass.

    If any command fails, investigate the error output, fix the issue (likely a missed import path or a lint rule violation), and re-run all four commands to confirm.
  </action>
  <verify>
    <automated>npm run build && npm run typecheck && npm run lint && npm run test && ls dist/index.js dist/index.cjs dist/index.d.ts dist/adapters/react.js dist/adapters/react.cjs dist/adapters/react.d.ts</automated>
  </verify>
  <done>npm run build, typecheck, lint, and test all exit cleanly. dist/ contains ESM + CJS + .d.ts files for both main entry and React subpath. All 26 tests pass.</done>
</task>

</tasks>

<verification>
Phase gate -- all must pass:
1. `npm run build` exits 0 and produces dist/index.js, dist/index.cjs, dist/index.d.ts, dist/adapters/react.js, dist/adapters/react.cjs, dist/adapters/react.d.ts
2. `npm run typecheck` exits 0
3. `npm run lint` exits 0 (checks src/, tests/, examples/)
4. `npm run test` exits 0 with all 26 tests passing
</verification>

<success_criteria>
- All source files reside in src/ with React adapter at src/adapters/react.ts
- Test files reside in tests/ with correct ../src/ imports
- Example files reside in examples/ with ../src/ relative imports using .js extension
- Changeset config at .changeset/config.json
- npm run build, typecheck, lint, test all pass cleanly
- dist/ contains index.js, index.cjs, index.d.ts, adapters/react.js, adapters/react.cjs, adapters/react.d.ts
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-pipeline/01-01-SUMMARY.md`
</output>
