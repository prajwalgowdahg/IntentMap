---
phase: 03-engine-dom-fixes
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/matcher.ts
  - src/IntentMap.ts
  - tests/engine-fixes.test.ts
autonomous: true
requirements: [BUG-02, BUG-03, BUG-04]

must_haves:
  truths:
    - "match() with no registered intents returns a clean no-match result (matched: false, intent: null, confidence: 0)"
    - "extractText() uses textContent before innerText, avoiding layout reflow"
    - "Tokenization is computed once per match() call, not twice"
  artifacts:
    - path: "src/matcher.ts"
      provides: "Matcher.match() with single tokenization pass"
      contains: "inputStems"
    - path: "src/IntentMap.ts"
      provides: "extractText() with textContent priority"
      contains: "textContent ?? innerText"
    - path: "tests/engine-fixes.test.ts"
      provides: "Tests for no-intent match, tokenization efficiency, textContent priority"
  key_links:
    - from: "src/matcher.ts"
      to: "src/embeddings.ts"
      via: "buildVector now receives pre-computed stems"
      pattern: "buildVector.*inputStems"
    - from: "src/IntentMap.ts"
      to: "src/matcher.ts"
      via: "match() delegates to Matcher.match()"
      pattern: "this\\.matcher\\.match"
---

<objective>
Fix three engine-level bugs: double tokenization in matcher, textContent priority in extractText, and no-intent edge case handling.

Purpose: These are performance and correctness bugs in the core matching engine. Double tokenization wastes CPU on every match() call. innerText triggers layout reflow. The no-intent edge case needs verification and a regression test.
Output: Fixed matcher.ts with single tokenization, fixed extractText(), and regression tests for all three bugs.
</objective>

<execution_context>
@/Users/prajwalhg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prajwalhg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-engine-dom-fixes/03-CONTEXT.md
@src/matcher.ts
@src/IntentMap.ts
@src/embeddings.ts
@src/tokenizer.ts
@tests/intentmap.test.ts
@tests/validation.test.ts

<interfaces>
<!-- Key interfaces the executor needs -->

From src/types.ts:
```typescript
export interface MatchResult {
  matched: boolean
  intent: string | null
  confidence: number
  scores: Record<string, number>
  input: string
}
```

From src/embeddings.ts:
```typescript
export function buildVector(text: string, caseSensitive?: boolean): TokenVector
// Currently calls tokenize().map(stem) internally -- will need overload
```

From src/tokenizer.ts:
```typescript
export function tokenize(text: string, caseSensitive?: boolean): string[]
export function stem(word: string): string
```
</interfaces>
</context>

<feature>
  <name>Engine bug fixes</name>
  <files>src/matcher.ts, src/IntentMap.ts, tests/engine-fixes.test.ts</files>
  <behavior>
    BUG-03 (double tokenization):
    - matcher.ts match() currently calls buildVector(input) which internally calls tokenize().map(stem), then calls tokenize().map(stem) again on line 44
    - Fix: compute inputStems once before the loop, pass inputStems to a modified buildVector that accepts pre-computed stems
    - OR: keep buildVector as-is (it builds the ngram vector which is separate from stem computation), and just remove the redundant tokenize+stem on line 44 by reusing the stems from buildVector
    - Simplest approach: compute inputStems = tokenize(input).map(stem) once, then compute inputVec from inputStems without re-tokenizing, pass inputStems to bestKeywordScore

    BUG-02 (textContent priority):
    - IntentMap.ts line 242: change `target.innerText ?? target.textContent` to `target.textContent ?? target.innerText`
    - textContent avoids layout reflow that innerText causes

    BUG-04 (no-intent edge case):
    - matcher.ts line 61 already has `sorted[0] ?? [null, 0]` which handles empty scores
    - When store has no intents, Object.entries(scores) is empty, sorted is [], sorted[0] is undefined, guard kicks in
    - This already works -- just needs a regression test to lock the behavior
  </behavior>
  <implementation>
    1. RED: Write tests/engine-fixes.test.ts with:
       - Test: match() on empty IntentMap (no intents) returns { matched: false, intent: null, confidence: 0, scores: {}, input }
       - Test: extractText called on element with both textContent and innerText returns textContent value (mock element)
       - Test: verify buildVector is called with correct args, inputStems not recomputed (spy on tokenize)
    2. GREEN: Fix the bugs to pass tests:
       - In matcher.ts match(): compute inputStems once before loop, use for both buildVector input and bestKeywordScore
       - Need to either: (a) add an overload to buildVector that accepts pre-computed stems, or (b) inline the vector building. Approach (a) is cleaner.
       - In IntentMap.ts extractText(): swap textContent and innerText order
    3. REFACTOR: Clean up any duplication
  </implementation>
</feature>

<verification>
  All tests pass: `npx vitest run`
  TypeScript compiles: `npx tsc --noEmit`
  Lint passes: `npx eslint src/ tests/`
  Specifically verify: `npx vitest run tests/engine-fixes.test.ts`
</verification>

<success_criteria>
1. `match()` on an IntentMap with zero intents returns `{ matched: false, intent: null, confidence: 0, scores: {}, input }` without error
2. `extractText()` returns `textContent` before checking `innerText`
3. `tokenize()` is called exactly once per `match()` call (not twice)
4. All existing 58 tests continue to pass
5. New test file `tests/engine-fixes.test.ts` with at least 3 test cases
</success_criteria>

<output>
After completion, create `.planning/phases/03-engine-dom-fixes/03-01-SUMMARY.md`
</output>
